import type { Metadata } from "next";
import { Cormorant_Garamond, Inter } from "next/font/google";
import "./globals.css";

const cormorant = Cormorant_Garamond({
  variable: "--font-cormorant",
  subsets: ["latin"],
  weight: ["300", "400", "500"],
  display: "swap",
});

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  weight: ["400", "500"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "Viajes MG — Agencia de Viajes en CDMX | República Mexicana",
  description:
    "Agencia de viajes boutique en Ciudad de México. Más de 15 años diseñando experiencias únicas por toda la República Mexicana. Viajes personalizados, grupos y corporativos.",
  keywords: ["agencia de viajes cdmx", "viajes mexico", "tours mexico", "viajes boutique", "agencia viajes ciudad de mexico"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="es"
      className={`${cormorant.variable} ${inter.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-background text-foreground">
        {children}
      </body>
    </html>
  );
}
